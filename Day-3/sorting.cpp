#include <bits/stdc++.h>
using namespace std;
class Hello
{
    int x;
};

int main()
{
    // int *a = new int[10];
    // for (int i = 0; i < 10; i++)
    // {
    //     cin >> *(a + i);
    // }

    // sort(a, a + 10);
    // // sort(a, a + 10);
    // // sort(a, a + 10, greater<int>());

    // for (int i = 0; i < 10; i++)
    // {
    //     cout << *(a + i) << " ";
    // }

    Hello s1;

    cout << sizeof(s1) << endl;
    return 0;
}