#include <bits/stdc++.h>
using namespace std;

int main()
{
    float emu = 3.24;
    string s;
    cin >> emu;
    // getchar();
    cin.ignore();
    getline(cin, s);
    cout << emu << endl;
    cout << s;
    // string::iterator it;

    // s.assign("Emu");
    // s.erase();
    // s.clear();
    // cout << *s.begin() << endl;
    // cout << *s.end() << endl;
    // cout << s << endl;

    // for (auto it = s.begin(); it < s.end(); it++)
    // {
    //     cout << *it << endl;
    // }
    // s.insert(1, " ");
    // s.replace(1, 4, "");

    // cout << s.size() << endl;
    // cout << s << endl;
    // s.resize(100, ' ');
    // cout << s.capacity() << endl;
    // cout << s << endl;

    // // s.clear();
    // cout << s.empty();
    // capacity,size,maxsize
    //  cout << str.size() << endl;
    //  cout << str.capacity() << endl;
    //  cout << str.max_size() << endl;
    //  str = "jsjxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx";
    //  cout << str.size() << endl;
    //  cout << str.capacity() << endl;
    //  str = "xxxxx";
    //  cout << str.size() << endl;
    //  cout << str.capacity() << endl;

    return 0;
}